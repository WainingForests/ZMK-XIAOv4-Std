# Trackball half: respond every connection event (7.5ms) instead of
# skipping up to 30. Default latency of 30 throttles pointer reports
# to ~4/sec. Costs battery on this half only.
CONFIG_ZMK_SPLIT_BLE_PREF_LATENCY=0
